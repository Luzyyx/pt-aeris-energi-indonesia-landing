import ImageFanCards from './ImageFanCards';

// Komponen ini menjadi entry point untuk SocialCards dari snippet 21st.dev.
// Implementasi animasi fan, hover, dan pagination ada di ImageFanCards.
export default function SocialCards(props) {
  return <ImageFanCards {...props} />;
}
